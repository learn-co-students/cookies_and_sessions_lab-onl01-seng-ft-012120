<h2>New item"</h2>
<%= form_tag show_cart(cart), method: "post" do %>
<div class="field">
    <%= label_tag "product", "Product" %>
    <%= text_field_tag "product", product %>
</div>

<%= submit_tag "add to cart" %>
<% end %>